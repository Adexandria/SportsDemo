@page "/admin/products/details/{id:long}"

<h3 class="bg-info text-white text-center p-1">Details</h3>

<table class="table table-sm table-bordered table-striped">
    <tbody>
        <tr><th>ID</th><td>@Models</td></tr>
        <tr><th>Name</th><td>@Models.Details.Name</td></tr>
        <tr><th>Description</th><td>@Models.Detail.</td></tr>
        <tr><th>Category</th><td>@Detail.Category</td></tr>
        <tr><th>Price</th><td>@Detail.Price.ToString("C")</td></tr>
    </tbody>
</table>

<NavLink class="btn btn-warning" href="@EditUrl">Edit</NavLink>
<NavLink class="btn btn-secondary" href="/admin/products">Back</NavLink>

@code {

    [Inject]
    public IData Repository { get; set; }

    [Parameter]
    public long Id { get; set; }

    public Details Product { get; set; }

    protected override void OnParametersSet()
    {
        Product = Repository.Products.FirstOrDefault(p => p.ProductID == Id);
    }

    public string EditUrl => $"/admin/products/edit/{Product.ProductID}";
}